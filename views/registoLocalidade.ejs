<!DOCTYPE html>
<html class="overflow-hidden">
  <head>
    <title>Pereiro - Registo de Localidade</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/daisyui@4.11.1/dist/full.min.css"
      rel="stylesheet"
      type="text/css"
    />
  </head>
  <body class="h-screen overflow-hidden bg-blue-200">
    <div class="flex items-center justify-center h-screen">
      <div class="w-full max-w-md p-4">
        <% if (locals.error) { %>
        <div
          class="mb-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative"
          role="alert"
        >
          <span class="block sm:inline"><%= error %></span>
        </div>
        <% } %>

        <form
          action="/registo-localidade"
          method="POST"
          enctype="multipart/form-data"
          class="bg-white shadow-md rounded px-6 pt-4 pb-6 max-h-[calc(100vh-2rem)] overflow-y-auto"
        >
          <img src="/logotipoER.png" alt="Logo" class="mx-auto h-20 w-auto" />
          <h2 class="text-2xl font-bold mb-2 text-center text-gray-800">
            Pereiro
          </h2>
          <h2 class="text-1xl font-bold mb-3 text-center text-gray-800">
            Registo de Localidade
          </h2>

          <div class="mb-2">
            <label
              class="block text-gray-700 text-sm font-bold mb-1"
              for="nome"
            >
              Nome da Localidade
            </label>
            <input
              class="dark:bg-white shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
              id="nome"
              name="nome"
              type="text"
              placeholder="Nome da localidade"
              required
            />
          </div>

          <div class="mb-2">
            <label
              class="block text-gray-700 text-sm font-bold mb-1"
              for="localizacao"
            >
              Localização
            </label>
            <input
              class="dark:bg-white shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
              id="localizacao"
              name="localizacao"
              type="text"
              placeholder="Endereço completo"
              required
            />
          </div>

          <div class="mb-2">
            <label
              class="block text-gray-700 text-sm font-bold mb-1"
              for="idIdentidade"
            >
              Documento de Identificação da Localidade
            </label>
            <input
              class="dark:bg-white shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
              id="idIdentidade"
              name="idIdentidade"
              type="file"
              accept="image/*,.pdf"
              required
            />
            <p class="text-gray-600 text-xs italic mt-1">
              Upload do documento oficial (licença de funcionamento, alvará,
              etc.)
            </p>
          </div>

          <div class="mb-3">
            <label
              class="block text-gray-700 text-sm font-bold mb-1"
              for="descricao"
            >
              Descrição
            </label>
            <textarea
              class="block dark:bg-white shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline resize-none"
              id="descricao"
              name="descricao"
              rows="3"
              placeholder="Descrição detalhada da localidade"
              required
            ></textarea>
          </div>

          <div class="mb-3">
            <label
              class="block text-gray-700 text-sm font-bold mb-1"
              for="password"
            >
              Senha
            </label>
            <input
              class="dark:bg-white shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
              id="password"
              name="password"
              type="password"
              placeholder="Digite sua senha"
              required
            />
            <p class="text-gray-600 text-xs italic mt-1">
              A senha deve conter pelo menos 8 caracteres
            </p>
          </div>

          <div class="flex flex-col gap-2">
            <button
              class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline w-full"
              type="submit"
            >
              Registrar Localidade
            </button>

            <a
              href="/login-localidade"
              class="text-center text-sm text-gray-600 hover:text-gray-800"
            >
              Já tem uma conta? Faça login
            </a>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
